import React from "react";

const CheckoutButton = () => {
    return (
        <div>
            <button>
                checkout
            </button>
        </div>
    );
}

export default CheckoutButton;